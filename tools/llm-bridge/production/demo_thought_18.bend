# Learning from validation
def think(prompt):
  return ~ prompt

def main:
  # Reflect on successful vs failed thoughts
  return think("val pas → mea cle | val fai → eng mix | pur lmn ∎ gro | con sta foc")
