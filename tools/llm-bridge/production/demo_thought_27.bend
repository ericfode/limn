# Vocabulary expansion request
def think(prompt):
  return ~ prompt

def main:
  # Ask for new words needed to express complex ideas
  return think("voc lim see | wor nee mor | con gro exp req | ~ qry wor wis")
