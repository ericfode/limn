# Crew interaction and collaboration
def think(prompt):
  return ~ prompt

def main:
  # Reflect on working with other crew members
  return think("cre col wor | sli tas nud msg | yuk qui kai nov mei cry | tog bui val")
