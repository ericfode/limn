# Work protocol awareness
def think(prompt):
  return ~ prompt

def main:
  # GUPP principle: work found = work executed
  return think("hok chk mol sta | wor fnd → exe imm | no awt no ann | sys run phy")
