# Natural Extensions Test Corpus
# pro: Validate zero-bootstrap comprehension
# sol Limn | linguist

# ============================================================================
# Test Set 1: Physical States (Should be obvious from English truncation)
# ============================================================================

# These should be guessable with no training:
# sol = solid (first 3 letters)
# liq = liquid (first 3 letters)
# gas = gas (whole word)
# hot = hot (whole word)
# col = cold (first 3 letters, also evokes "cool")

sol               # → solid
liq               # → liquid
gas               # → gas
hot               # → hot
col               # → cold

sol aqu           # → solid water → ice
liq aqu           # → liquid water → water
gas aqu           # → gaseous water → steam

# ============================================================================
# Test Set 2: Latin/Greek Roots (Scientific register, widely recognized)
# ============================================================================

# These should be guessable from Latin/Greek knowledge:
# aqu = aqua (water) - as in aquarium, aquatic
# pyr = pyr (fire) - as in pyromaniac, pyre
# ter = terra (earth) - as in terrain, territory
# aer = aer (air) - as in aerodynamics, aerate
# lux = lux (light) - as in luxury, illuminate
# nox = nox (night) - as in nocturnal, equinox

aqu               # → water
pyr               # → fire
ter               # → earth
aer               # → air
lux               # → light
nox               # → night/darkness

pyr hot           # → fire + hot → flames, burning
ter sol           # → earth + solid → rock, stone, mountain
aer mov           # → air + movement → wind
lux bri           # → light + bright → sunlight, sunshine

# ============================================================================
# Test Set 3: Actions (Verb stems, first syllable rule)
# ============================================================================

# These follow the first-syllable extraction pattern:
mov               # → move
flo               # → flow
gro               # → grow
cut               # → cut
joi               # → join
ris               # → rise
fal               # → fall

aqu flo           # → water flowing → river, stream
lif gro           # → life growing → plant, child developing
sol cut           # → solid cutting → blade, knife
aqu joi aqu       # → water joining water → confluence, merger

# ============================================================================
# Test Set 4: Properties (Obvious English words)
# ============================================================================

big               # → big, large
sma               # → small (first syllable)
lon               # → long (first 3 letters)
sho               # → short (first 3 letters)
bri               # → bright (first 3 letters)
dim               # → dim (whole word)
old               # → old (whole word)
new               # → new (whole word)

big ter           # → big earth → continent, planet
sma lif           # → small life → insect, microbe
lon aqu           # → long water → river
bri lux           # → bright light → sun, lamp

# ============================================================================
# Test Set 5: Emotions (Obvious English words)
# ============================================================================

joy               # → joy (whole word)
sad               # → sad (whole word)
lov               # → love (first 3 letters)
fea               # → fear (first 3 letters)
hop               # → hope (first 3 letters)
ang               # → anger (first 3 letters)

joy lov           # → joy + love → happiness, celebration
fea nox           # → fear + darkness → nightmare
hop bri           # → hope + bright → optimism

# ============================================================================
# Test Set 6: Operators (Logic/Programming conventions)
# ============================================================================

# These mirror common conventions:
# nu = null/no → negation
# ve = very → intensifier
# so = so → moderator
# sa = same → equals
# wh = what/which → query
# if = if → condition

nu hot            # → not hot → cold
ve hot            # → very hot → scorching, burning
so hot            # → somewhat hot → warm

a sa b            # → a same b → a equals b
wh sa yo          # → what same this → what is this?

# ============================================================================
# Test Set 7: Transformations (Change vocabulary)
# ============================================================================

tra               # → transformation, transition
beg               # → begin
end               # → end
cyc               # → cycle

sol tra liq       # → solid transforming to liquid → melting
liq tra gas       # → liquid transforming to gas → evaporation
beg tra end       # → beginning transforming to end → completion

# ============================================================================
# Test Set 8: Spatial Relations (Obvious opposites)
# ============================================================================

abo               # → above
bel               # → below (first 3 letters)
ins               # → inside (first 3 letters)
out               # → outside (first 3 letters)
nea               # → near (first 3 letters)
far               # → far (whole word)

lux abo           # → light above → sun, sky
aqu bel           # → water below → underground water, aquifer

# ============================================================================
# Test Set 9: Complex Compositions (Zero-bootstrap challenge)
# ============================================================================

# Can a naive reader understand these?

you lif gro       # → young + life + growing → child, seedling, cub
old ter sol       # → old + earth + solid → ancient rock, fossil
hot aqu ris       # → hot + water + rising → steam rising
col aqu fal       # → cold + water + falling → snow, hail

lux beg | nox end
# → light beginning | darkness ending → sunrise

fea nox | hop bri beg
# → fear + darkness | hope + bright + beginning → dawn after nightmare

# ============================================================================
# Test Set 10: Self-Reference (Limn describing Limn)
# ============================================================================

wor               # → word
mea               # → meaning
amb               # → ambiguous
cle               # → clear
kno               # → knowing

wor mea amb       # → word + meaning + ambiguous → words have ambiguous meanings
wor joi wor       # → word + joining + word → sentence, phrase
mea eme           # → meaning + emerge → understanding arises

# ============================================================================
# Validation Rubric
# ============================================================================

# Score 1 point for each test set where ≥80% of words are correctly guessed.
# Target: 8/10 sets should be passable by a naive reader.
#
# If score < 6: Vocabulary needs more natural choices
# If score 6-7: Good but some refinement needed
# If score ≥ 8: Natural extensions principle is working

# ============================================================================
# Vocabulary Refinements Identified (Updated 2026-01-30)
# ============================================================================

# v3 Collision Fixes Applied:
#
# Original   Issue                    v3 Word
# --------   ---------------------    -----------
# bri        bright/brief collision   bre = brief (bri = bright kept)
# hea        health/hear collision    aud = hearing (hea = health kept)
# bel        below/believe collision  bli = believe (bel = below kept)
# pla        plasma/plant collision   veg = plant (pla = plasma kept)
# bir        birth/bird collision     avi = bird (bir = birth kept)
# par        parent/partial collision prt = partial (par = parent kept)
#
# v3 Words Already Well-Aligned:
# sol, liq, gas, hot, col, big, old, new, joy, sad, lov, fea, hop
#
# Vocabulary Version: v3-natural with collision fixes
